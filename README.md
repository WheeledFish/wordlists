# Merger script usage

This script was created in order to ease updating proccess of the wordlists.
It adds new unique entries from specified "new_file.txt" to the "original_file.txt" and removes empty lines.

```bash
python3 merger.py original_file.txt new_file.txt
```